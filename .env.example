# Copie ce fichier en .env et remplis la valeur

# Supabase → Settings → Database → Connection string → URI
DATABASE_URL=postgresql://postgres:[TON-MOT-DE-PASSE]@db.[TON-REF-PROJET].supabase.co:5432/postgres

# Port local (optionnel, défaut 3000)
PORT=3000
